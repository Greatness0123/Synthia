export const SUPABASE_BUCKETS = {
  FRAMES: 'synthia-frames',
  MODELS: 'synthia-models',
} as const;

export const SUPABASE_TABLES = {
  SESSIONS: 'sessions',
  MEMORIES: 'memories',
  SKILLS: 'skills',
  MOTOR_PROGRAMS: 'motor_programs',
  AGENT_IDENTITY: 'agent_identity',
  AGENT_IDENTITY_LOG: 'agent_identity_log',
  SCHEMA_META: 'schema_meta',
} as const;
